{{#each items as |item|}}
  <span type="button" class="btn btn-primary meal-item" data-id="{{item.id}}">
    {{item.name}}
  </span>
  <button type="button" class="btn btn-danger meal-item-button-delete float-right"  data-id="{{item.id}}">X</button>
  <br />
{{/each}}
